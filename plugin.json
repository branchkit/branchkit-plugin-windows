{
  "id": "windows",
  "name": "Windows",
  "capabilities": ["windows", "shell"],
  "version": "1.0.0",
  "description": "Native window management — snap, move to space, switch desktops, browser tab operations. Uses built-in OS primitives without a tiling system.",
  "author": "BranchKit",
  "run": "./windows-plugin",
  "store_data": {
    "voice_commands": "commands.json"
  },
  "action_prefix": "windows",
  "provides": {
    "tags": {
      "plugin.windows.snap_mode": {
        "label": "Snap Mode",
        "description": "Snap mode active — say direction words to snap windows without repeating 'snap'"
      }
    },
    "tag_namespaces": [
      {
        "prefix": "plugin.windows.snap_mode",
        "lifecycle": "transient",
        "clear_on": "_platform.speech.session_ended",
        "scoped": true,
        "access": "open"
      }
    ]
  },
  "implements": {
    "on_action": true,
    "settings_tabs": [{ "label": "Commands", "key": "commands" }],
    "render_settings": true
  }
}
